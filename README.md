# nix-dev-reference

A public, opinionated reference repository for:

- NixOS development environments
- Home Manager identity configuration
- Project-scoped toolchains
- Secret handling via pass + direnv

This repository contains **patterns**, not personal data.

### Agent diagnostics

Before running agent workflows:

```bash
scripts/agent-doctor.sh
```
